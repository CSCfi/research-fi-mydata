# This file is part of the research.fi service
#
# Copyright 2021 Ministry of Education and Culture, Finland
#
# :author: CSC - IT Center for Science Ltd., Espoo Finland servicedesk@csc.fi
# :license: MIT
apiVersion: v1
kind: Template
metadata:
  labels:
    app: researchfi-mydata-identityserver-devel
    template: researchfi-mydata-identityserver-devel
  name: researchfi-mydata-identityserver-devel
objects:
  # Service (devel)
  - apiVersion: v1
    kind: Service
    metadata:
      name: researchfi-mydata-identityserver-service-devel
      labels:
        app: researchfi-mydata-identityserver-devel
      annotations:
        description: Research.fi mydata identityserver service for devel-branch
    spec:
      ports:
        - name: mydata-identityserver-devel
          port: 8080
          targetPort: 8080
      selector:
        depcfg: mydata-identityserver-devel

  # ImageStream (development)
  - apiVersion: v1
    kind: ImageStream
    metadata:
      name: researchfi-mydata-identityserver-devel
      labels:
        app: researchfi-mydata-identityserver-devel

  # BuildConfig using Docker build strategy
  - apiVersion: v1
    kind: BuildConfig
    metadata:
      name: researchfi-mydata-identityserver-build-devel
      labels:
        app: researchfi-mydata-identityserver-devel
    spec:
      source:
        git:
          uri: https://github.com/CSCfi/research-fi-mydata.git
          ref: devel
        contextDir: .
      strategy:
        dockerStrategy:
          dockerfilePath: aspnetcore/openshift/identityserver/Dockerfile
      output:
        to:
          kind: ImageStreamTag
          name: researchfi-mydata-identityserver-devel:latest
      successfulBuildsHistoryLimit: 4
      failedBuildsHistoryLimit: 4

  # Deployment config (development)
  - apiVersion: v1
    kind: DeploymentConfig
    metadata:
      name: researchfi-mydata-identityserver-deployment-devel
      labels:
        app: researchfi-mydata-identityserver-devel
    spec:
      selector:
        app: researchfi-mydata-identityserver-devel
        depcfg: mydata-identityserver-devel
      template:
        metadata:
          labels:
            app: researchfi-mydata-identityserver-devel
            depcfg: mydata-identityserver-devel
        spec:
          containers:
            - name: mydata-identityserver-devel
              image: researchfi/researchfi-mydata-identityserver-devel
              imagePullPolicy: Always
              ports:
                - containerPort: 8080
                  protocol: TCP
      replicas: 1
      triggers:
        - type: ConfigChange
        - type: ImageChange
          imageChangeParams:
            automatic: true
            containerNames:
              - mydata-identityserver-devel
            from:
              kind: ImageStreamTag
              name: researchfi-mydata-identityserver-devel:latest
      strategy:
        type: Rolling